version: '3.8'

services:
  mongodb:
    image: mongo:4.4
    container_name: flyingo_db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:6.2-alpine
    container_name: flyingo_redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
